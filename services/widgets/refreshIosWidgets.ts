/**
 * Android / web stubs — widgets are iOS-only (expo-widgets).
 */
export function refreshIosWidgets(): void {
  return;
}

export function subscribePlayerToIosWidgets(): () => void {
  return () => {
    return;
  };
}
